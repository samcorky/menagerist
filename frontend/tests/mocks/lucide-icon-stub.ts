// Stub for @lucide/svelte/icons/* subpath imports (aliased via regex in vitest.config.ts).
// The calendar and other shadcn components use the per-icon subpath style; tests never
// render icons so a null default export is sufficient.
const stub = null as unknown as never;
export default stub;
