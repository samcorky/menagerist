@AGENTS.md

## Claude Code

- For any `.svelte` file or `.svelte.ts`/`.svelte.js` module, use the `svelte-file-editor` subagent.
- Run `poe check` before reporting a task complete if you've touched backend or frontend code.
- Do not commit unless asked.

LLAP 🖖
